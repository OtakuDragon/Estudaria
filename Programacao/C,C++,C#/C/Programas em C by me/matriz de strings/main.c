#include <stdio.h>
#include <stdlib.h>

int main()
{char nome [5][4];
int x,y;
for (x=0; x<=4; x++){
scanf ("%s",&nome[x][4]);
}

for (x=0; x<=4; x++){
printf ("%s\n",&nome[x][4]);
}
  system("PAUSE");	
  return 0;
}
